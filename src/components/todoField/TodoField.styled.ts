import { styled } from '@mui/system';

export const Wrapper = styled('div')({
    display: 'flex',
    width: '100%',
    gap: '10px',
    marginBottom: '20px',
});

export const WrapperField = styled('div')({
    width: '85%',
});
